{
  "name": "generate-secret-key",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "crypto": "^1.0.1"
  },
  "scripts": {
    "generate": "node -e 'console.log(require('crypto').randomBytes(256).toString('base64'));'"
  }
}
